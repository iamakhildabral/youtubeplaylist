# YouTube Playlist Video Downloader

This Python script is designed to download videos from a YouTube playlist with a specific resolution using the Pytube library. It simplifies the process of batch downloading videos and their associated subtitles.

## Prerequisites

Make sure you have the Pytube library installed. If not, you can install it using pip:

```bash
pip install pytube

